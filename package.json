{
  "name": "atomica-insurance-markets-rink",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ atomica-insurance-markets-rink",
    "create-local": "graph create --node http://localhost:8020/ atomica-insurance-markets-rink",
    "remove-local": "graph remove --node http://localhost:8020/ atomica-insurance-markets-rink",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 atomica-insurance-markets-rink"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
